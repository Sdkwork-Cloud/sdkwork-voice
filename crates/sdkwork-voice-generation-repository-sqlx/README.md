# sdkwork-voice-generation-repository-sqlx

SDKWork Voice SQLx repository and migration catalog for generation tasks,
artifacts, Drive sync rows, provider webhooks, and request logs.

Verification:

```powershell
cargo test -p sdkwork-voice-generation-repository-sqlx
```
