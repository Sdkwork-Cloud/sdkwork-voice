# sdkwork-voice-drive-sync-processor Specs

Component root: `crates/sdkwork-voice-drive-sync-processor`

Fetches provider artifact bytes and persists through `sdkwork-voice-artifact-drive-service` when Drive DB is configured.
