# sdkwork-voice-local-api-proxy-native-host

SDKWork Voice native host support for local API proxy configuration and
provider upstream URL/auth handling.

Verification:

```powershell
cargo test -p sdkwork-voice-local-api-proxy-native-host
```
