{
  "name": "@sdkwork/voice-drive-sync-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SDKWork Voice drive sync worker that drains pending artifact drive sync rows through the backend retry API.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sdkwork/voice-contracts": "workspace:*"
  },
  "sdkwork": {
    "workspace": "sdkwork-voice",
    "architecture": "common",
    "domain": "voice",
    "capability": "drive-sync-worker",
    "status": "ready"
  }
}
